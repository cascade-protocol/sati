# Helius API key for Solana RPC and ZK compression
VITE_HELIUS_API_KEY=your-helius-api-key

# Agent signer private key (base58 encoded 64-byte secret key)
# Used for signing echo responses and paying for server-side submissions
SATI_AGENT_SIGNER_KEY=your-base58-private-key
