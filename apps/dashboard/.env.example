# Solana RPC endpoints
# For production, use a dedicated RPC provider like Helius or Triton
# The Helius API key is extracted to use the DAS API for listing agents
VITE_MAINNET_RPC=https://mainnet.helius-rpc.com/?api-key=your-api-key
VITE_MAINNET_WS=wss://mainnet.helius-rpc.com/?api-key=your-api-key

# Helius API key for ZK compression (Photon) queries
# Used to query compressed feedback attestations from the browser
VITE_HELIUS_API_KEY=your-helius-api-key
