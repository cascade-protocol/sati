/**
 * Dashboard Constants
 *
 * Shared constants used across the dashboard application.
 */

/**
 * LocalStorage key for persisting the selected Solana network.
 * Used by NetworkSelector, main.tsx, and sati.ts for network persistence.
 */
export const NETWORK_STORAGE_KEY = "sati-network";
