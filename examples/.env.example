# Solana keypair JSON file (e.g. ~/.config/solana/id.json)
# Generate one with: solana-keygen new
KEYPAIR_PATH=~/.config/solana/id.json

# Solana RPC URL (optional - defaults to public endpoint for NETWORK)
# RPC_URL=https://api.devnet.solana.com

# Pinata JWT for IPFS uploads (optional - falls back to hosted uploader)
# Get one at https://www.pinata.cloud/
# PINATA_JWT=

# Network: mainnet, devnet, or localnet (default: devnet)
NETWORK=devnet

# Agent mint address (base58) for sati-sdk feedback examples
# Run sati-sdk/quick-start.ts first, then paste the mint address here
# AGENT_MINT=

# Agent ID in CAIP-2 format for sati-agent0-sdk examples
# Format: solana:<chainRef>:<mintAddress>
# Run sati-agent0-sdk/quick-start.ts first to get one
# AGENT_ID=solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1:<your-mint-address>
